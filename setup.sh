#!/bin/bash

# if homebrew is installed
#  brew install node
#  if mocha is not installed
#    npm install -g mocha
#  if jscoverage is not installed
#    git clone https://github.com/visionmedia/node-jscoverage.git
#    ./configure && make && make install
